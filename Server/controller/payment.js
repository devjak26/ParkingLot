const newPayment=async(req,res)=>{
    // get userName, garage Name, date & time slot, amount, veh. ID
}

const checkPayment=async(req,res)=>{
    // by userName check all payment history
}

module.exports={newPayment,checkPayment}